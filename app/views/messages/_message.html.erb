<div class='message'>
  <div>
    <span class='author'>
      <%= message.user.username %>
    </span>
    <span class='timestamp'>
      <%= message.created_at.strftime('%b %e, %l:%M %p') %>
    </span>
  </div>
  <div class="content" data-encrypted='true' data-sender='<%= message.user.username %>'>
    <%= message.content %>
  </div>
</div>
